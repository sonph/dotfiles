#!/bin/bash

ARG1=$1
SOURCE_FILE1="$HOME/.shexports"

if [[ $ARG1 = '-h' || $ARG1 = '--help' ]]; then
	echo "    Refresh/re-source environment variables from"
	echo "        - $SOURCE_FILE1"
else
	source $SOURCE_FILE1
fi